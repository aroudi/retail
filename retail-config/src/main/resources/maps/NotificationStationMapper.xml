<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="au.gov.nsw.railcorp.cimg.dao.NotificationStationDao">
    <!-- result maps -->
    <insert id="insert" parameterType="NotificationStation" useGeneratedKeys="true" >
        <selectKey keyProperty="id" resultType="java.lang.Long" order="BEFORE">
            SELECT NOTIFICATION_STATION_SEQ.nextVal from dual
        </selectKey>
        INSERT INTO notification_station(
            NOTIF_STATION_ID,
            NOTIFICATION_ID,
            STATION_CAT_ID,
            STATION_TYPE
        ) VALUES (
          #{id},
          #{notificationId},
          #{stopId},
          #{stationType}
        )
    </insert>

    <delete id="delete" >
        DELETE
          From notification_station
        WHERE
          NOTIFICATION_ID = #{value}
    </delete>

</mapper>